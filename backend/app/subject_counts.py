MIN_SUBJECT_RELEVANCE_SCORE = 40
# Policy version for subject_catalog_counts. Must match the version written by
# scraper/_fix_subject_counts.py (title-matched counts per subject).
STRICT_COUNT_POLICY_VERSION = "detail-match-v1"
